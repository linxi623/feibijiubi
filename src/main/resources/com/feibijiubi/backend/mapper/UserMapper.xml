<!--定义Mapper映射文件的约束和基本结构-->
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.feibijiubi.backend.mapper.UserMapper">
    <select id="countByUsername" resultType="int">
        SELECT COUNT(*)
        FROM users
        WHERE username = #{username}
    </select>

    <insert id="createUser"
            parameterType="com.feibijiubi.backend.entity.User"
            useGeneratedKeys="true"
            keyProperty="id">
        INSERT INTO users (
            username,
            password_hash,
            nickname
        ) VALUES (
            #{username},
            #{passwordHash},
            #{nickname}
        )
    </insert>

    <select id="selectByUsernameForLogin"
            resultType="com.feibijiubi.backend.entity.User">
        SELECT
            id,
            username,
            password_hash,
            nickname,
            avatar_url,
            background_url,
            gender,
            description,
            experience,
            coin,
            vip,
            status,
            role,
            token_version,
            auth,
            auth_msg,
            created_at,
            updated_at,
            deleted_at
        FROM users
        WHERE username = #{username}
          AND deleted_at IS NULL
    </select>

    <select id="selectById"
            resultType="com.feibijiubi.backend.entity.User">
        SELECT
            id,
            username,
            nickname,
            password_hash,
            avatar_url,
            background_url,
            gender,
            description,
            experience,
            coin,
            vip,
            status,
            role,
            token_version,
            auth,
            auth_msg,
            created_at,
            updated_at,
            deleted_at
        FROM users
        WHERE id = #{id}
          AND deleted_at IS NULL
    </select>

    <select id="selectAuthStateById"
            resultType="com.feibijiubi.backend.entity.User">
        SELECT
            id,
            status,
            role,
            token_version,
            deleted_at
        FROM users
        WHERE id = #{id}
          AND deleted_at IS NULL
    </select>

    <update id="updateProfileById">
        UPDATE users
        <set>
            <if test="nickname != null and nickname != ''">nickname = #{nickname},</if>
            <if test="gender != null">gender = #{gender},</if>
            <if test="description != null">description = #{description},</if>
            <if test="updatedAt != null">updated_at = #{updatedAt},</if>
        </set>
        where id = #{id}
            AND deleted_at IS NULL
    </update>

    <update id="updatePassword">
        UPDATE users
        SET password_hash = #{passwordHash},
            token_version = token_version + 1
        WHERE id = #{id}
          AND deleted_at IS NULL
    </update>

    <update id="updateAvatar">
        UPDATE users
        <set>
            <if test="avatarUrl != null and avatarUrl != ''">avatar_url = #{avatarUrl},</if>
        </set>
        WHERE id = #{currentUserId}
            AND deleted_at IS NULL
    </update>

    <update id="updateBackground">
        UPDATE users
        SET background_url = #{backgroundUrl},
            updated_at = NOW()
        WHERE id = #{currentUserId}
          AND deleted_at IS NULL
    </update>

    <update id="decreaseCoin">
        UPDATE users
        SET coin = coin - #{coin}
        WHERE id = #{currentUserId}
            AND coin >= #{coin}
            AND deleted_at IS NULL
    </update>

    <update id="increaseCoin">
        UPDATE users
        SET coin = coin + #{coin}
            WHERE id = #{currentUserId}
                AND deleted_at IS NULL
    </update>

</mapper>
